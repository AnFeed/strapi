'use strict';

/**
 * distribuitor-image router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::distribuitor-image.distribuitor-image');
