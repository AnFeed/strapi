'use strict';

/**
 * distribuitor-image service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::distribuitor-image.distribuitor-image');
